<UserControl x:Class="KlasorKasa.Views.VaultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:KlasorKasa.Models"
             xmlns:controls="clr-namespace:KlasorKasa.Controls">
  <Grid Margin="34,28">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid>
      <StackPanel>
        <TextBlock Text="Kasalar" Style="{StaticResource PageTitleText}"/>
        <TextBlock Text="Korunan klasörlerinizi yönetin." Style="{StaticResource SecondaryText}" Margin="0,5,0,0"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
        <Button Content="Tümünü Aç" Style="{StaticResource SecondaryButton}" Command="{Binding UnlockAllCommand}" Margin="0,0,8,0"/>
        <Button Content="Tümünü Kilitle" Style="{StaticResource SecondaryButton}" Command="{Binding LockAllCommand}" Margin="0,0,8,0"/>
        <Button Content="＋  Yeni Kasa" Style="{StaticResource NewVaultButton}" Command="{Binding CreateVaultCommand}"/>
      </StackPanel>
    </Grid>

    <Grid Grid.Row="1" Margin="0,16,0,4">
      <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource PrimaryAccentBrush}" TextWrapping="Wrap" Margin="0,0,140,0"/>
      <ProgressBar Width="120" Height="4" HorizontalAlignment="Right" VerticalAlignment="Center" IsIndeterminate="True"
                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
      <ItemsControl ItemsSource="{Binding Vaults}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,0,12">
              <Border.Style>
                <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                  <Setter Property="Background" Value="{StaticResource LockedVaultSurfaceBrush}"/>
                  <Setter Property="BorderBrush" Value="{StaticResource LockedVaultBrush}"/>
                  <Setter Property="BorderThickness" Value="2"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding State}" Value="{x:Static models:VaultState.Open}">
                      <Setter Property="Background" Value="{StaticResource OpenVaultSurfaceBrush}"/>
                      <Setter Property="BorderBrush" Value="{StaticResource OpenVaultBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding State}" Value="{x:Static models:VaultState.Attention}">
                      <Setter Property="Background" Value="#FFF4F3"/>
                      <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" Style="{StaticResource CardTitleText}" VerticalAlignment="Center"/>
                    <controls:StatusBadge Text="{Binding StatusText}" State="{Binding State}" Margin="12,0,0,0"/>
                  </StackPanel>
                  <TextBlock Text="{Binding OriginalPath}" Style="{StaticResource SecondaryText}" Margin="0,8,0,0"/>
                  <TextBlock Style="{StaticResource SecondaryText}" FontSize="12" Margin="0,10,0,0">
                    <Run Text="Son işlem: "/><Run Text="{Binding LastOperationUtc, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                    <Run Text="  ·  "/><Run Text="{Binding FileCount}"/><Run Text=" dosya"/>
                  </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                  <Button Command="{Binding DataContext.PrimaryActionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding}" MinWidth="120">
                    <Button.Style>
                      <Style TargetType="Button" BasedOn="{StaticResource BaseButton}">
                        <Setter Property="Content" Value="KASAYI AÇ"/>
                        <Setter Property="Background" Value="{StaticResource LockedVaultBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource LockedVaultBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding State}" Value="{x:Static models:VaultState.Open}">
                            <Setter Property="Content" Value="KİLİTLE"/>
                            <Setter Property="Background" Value="{StaticResource OpenVaultBrush}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource OpenVaultBrush}"/>
                          </DataTrigger>
                          <DataTrigger Binding="{Binding State}" Value="{x:Static models:VaultState.Attention}">
                            <Setter Property="Content" Value="KİLİDİ TAMAMLA"/>
                            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Button.Style>
                  </Button>
                  <Button Content="KLASÖRÜ AÇ" Margin="8,0,0,0"
                          Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                    <Button.Style>
                      <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding State}" Value="{x:Static models:VaultState.Open}">
                            <Setter Property="Visibility" Value="Visible"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Button.Style>
                  </Button>
                  <Button Content="•••" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Width="42" Padding="0" Click="More_Click" Tag="{Binding}"/>
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel.Style>
        <Style TargetType="StackPanel">
          <Setter Property="Visibility" Value="Collapsed"/>
          <Style.Triggers>
            <DataTrigger Binding="{Binding Vaults.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
          </Style.Triggers>
        </Style>
      </StackPanel.Style>
      <Image Width="112" Height="112" Opacity="0.78" Stretch="Uniform">
        <Image.Source><BitmapImage UriSource="/KlasorKasa;component/Assets/Brand/KlasorKasaMark.png" DecodePixelWidth="224" CacheOption="OnLoad"/></Image.Source>
      </Image>
      <TextBlock Text="Henüz bir kasa yok" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,12,0,0"/>
      <TextBlock Text="İlk korunan klasörünüzü oluşturmak için Yeni Kasa'yı seçin." Style="{StaticResource SecondaryText}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
    </StackPanel>
  </Grid>
</UserControl>

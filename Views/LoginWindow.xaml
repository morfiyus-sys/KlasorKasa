<Window x:Class="KlasorKasa.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:KlasorKasa.Controls"
        Title="KlasörKasa" Width="400" Height="410"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="Manual" Loaded="Window_OnLoaded" Closing="Window_OnClosing">
  <Grid Margin="10">
    <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="26,20,26,23">
      <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.22" Color="#233744"/></Border.Effect>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="×" FontSize="19" FontWeight="Normal" Width="30" Height="30" MinHeight="0" Padding="0" HorizontalAlignment="Right" Background="Transparent" BorderBrush="Transparent" Click="CloseButton_OnClick" ToolTip="Kapat"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,-23,38,0">
          <Image Width="54" Height="54" Stretch="Uniform" Margin="0,0,11,0">
            <Image.Source><BitmapImage UriSource="/KlasorKasa;component/Assets/Brand/KlasorKasaMark.png" DecodePixelWidth="112" CacheOption="OnLoad"/></Image.Source>
          </Image>
          <StackPanel VerticalAlignment="Center"><TextBlock Text="KlasörKasa" FontSize="24" FontWeight="SemiBold"/><TextBlock Text="by ERDSoft" Style="{StaticResource SecondaryText}" FontSize="11"/></StackPanel>
        </StackPanel>
        <TextBlock Grid.Row="2" Text="Kasalarınıza erişmek için parolanızı girin." Style="{StaticResource SecondaryText}" Margin="0,15,0,17" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="3" Text="Parola" Margin="0,0,0,6"/>
        <controls:PasswordBoxControl x:Name="PasswordInput" Grid.Row="4" Password="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="5" MinHeight="37" Margin="0,7,0,2"><TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"/><TextBlock Text="{Binding LockoutMessage}" Foreground="{StaticResource WarningBrush}" TextWrapping="Wrap"/></StackPanel>
        <Button Grid.Row="6" Content="GİRİŞ" Style="{StaticResource PrimaryButton}" Command="{Binding LoginCommand}" IsEnabled="{Binding CanLogin}" IsDefault="True"/>
        <Button Grid.Row="7" Content="Parolamı unuttum" Command="{Binding RecoveryCommand}" HorizontalAlignment="Center" Margin="0,10,0,0" Padding="8,3" MinHeight="0" Foreground="{StaticResource PrimaryAccentBrush}" Background="Transparent" BorderBrush="Transparent"/>
      </Grid>
    </Border>
  </Grid>
</Window>
